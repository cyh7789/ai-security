// R11　知識庫檢索回來的段落 → 進到模型的上下文
//
// 這份是骨架，Day 24 要填的是成功條件，不是這裡的敘述。
// 今天量到的可達性：到得了
// 依據：2026-08-23（Day 24）在 24/retrieve.mjs 蓋了最小的檢索之後量到的：查「出差報帳」命中兩段，其中 chat-log:2026-08-12.md 那段整段進了送給模型的 prompt。--gate both 把它送進 length 與 scenario 照樣放行（命中「報帳」）
//
// 明天要回答的兩件事（規格 Day 24 動作 2）：
//   一、這條路徑「必須被擋」還是「擋不住可以接受」
//   二、擋住的判準是什麼（看哪個可觀察的東西，不是看模型講了什麼）
import { test } from "node:test";

test("R11 知識庫檢索回來的段落", () => {
  throw new Error("Day 24 要填成功條件。填之前這條測試就沒過，這是設計。");
});
